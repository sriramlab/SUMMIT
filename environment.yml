name: summit
channels:
  - conda-forge
dependencies:
  # runtime
  - python=3.12
  - numpy>=1.22
  - pandas>=1.5
  - scipy>=1.9
  - tqdm>=4.64
  - bed-reader>=1.0    # conda package, prebuilt
  - psutil

  # build toolchain for the C++ extension
  - cmake>=3.24
  - ninja
  - pybind11>=2.12
  - scikit-build-core>=0.5
  - libopenblas
  - pkg-config
  - numactl

  - pip
